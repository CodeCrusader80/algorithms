//Simple implementation of Linear Search. It is probably one of the simplest search algorithms to understand and implement.

function linearSearch(arr, target) {
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === target) {
            return i;
        }
    }
    return -1;
}